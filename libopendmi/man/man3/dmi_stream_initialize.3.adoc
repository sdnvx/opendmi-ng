= dmi_stream_initialize(3)
The OpenDMI contributors
OpenDMI 0.5.0

:doctype: manpage
:release-version: 0.5.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_stream_initialize - initialize a stream for reading an entity's body data.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/stream.h>

bool dmi_stream_initialize(dmi_stream_t *stream, const dmi_entity_t *entity);
----

== Description

The `dmi_stream_initialize` function sets up the stream to read the body of
`entity`, positioning the cursor at the beginning of the body (past the SMBIOS
structure header).

=== Parameters

`stream`::
Stream to initialize.

`entity`::
Entity whose body data will be read.

== Return value

Returns `true` on success, `false` if `stream` or `entity` is `nullptr`.

== See also

`dmi_stream_seek`(3),
`dmi_stream_reset`(3),
`dmi_stream_read_data`(3),
`dmi_stream_is_done`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
