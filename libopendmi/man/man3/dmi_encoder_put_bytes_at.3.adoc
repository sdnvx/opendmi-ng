= dmi_encoder_put_bytes_at(3)
The OpenDMI contributors
OpenDMI 0.5.0

:doctype: manpage
:release-version: 0.5.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_encoder_put_bytes_at - write bytes of a structure at a given offset.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/encoder.h>

bool dmi_encoder_put_bytes_at(
        dmi_encoder_t *encoder,
        const void    *ptr,
        size_t         offset,
        size_t         length);
----

== Description

The `dmi_encoder_put_bytes_at` function writes `length` bytes at `ptr` at
`offset` from the beginning of the structure, without moving the position.

Only the bytes written already may be written again, which is how a value
the rest of the structure decides, such as a length, is filled in once it is
known.

=== Parameters

`encoder`::
Encoder to write to.

`ptr`::
Bytes to write.

`offset`::
Byte offset from the beginning of the structure.

`length`::
Number of the bytes.

== Return value

`true` on success, `false` otherwise.

== Errors

`DMI_ERROR_OUT_OF_MEMORY`::
Buffer of the writer cannot grow.

== See also

`dmi_encoder_put_bytes`(3),
`dmi_encoder_put_at`(3),
`dmi_encoder_finish`(3),
`dmi_writer_put_bytes_at`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
