= dmi_stream_seek(3)
The OpenDMI contributors
OpenDMI 0.5.0

:doctype: manpage
:release-version: 0.5.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_stream_seek - seek to a byte position within the stream.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/stream.h>

bool dmi_stream_seek(dmi_stream_t *stream, size_t position);
----

== Description

The `dmi_stream_seek` function repositions the stream cursor to `position`
bytes from the beginning of the entity body.

=== Parameters

`stream`::
Stream to reposition.

`position`::
Byte offset from the beginning of the entity body.

== Return value

Returns `true` on success, `false` if `position` is beyond the end of the
stream.

== See also

`dmi_stream_initialize`(3),
`dmi_stream_reset`(3),
`dmi_stream_is_done`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
