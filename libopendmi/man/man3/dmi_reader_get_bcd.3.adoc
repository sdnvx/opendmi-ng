= dmi_reader_get_bcd(3)
The OpenDMI contributors
OpenDMI 0.5.0

:doctype: manpage
:release-version: 0.5.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_reader_get_bcd - read and decode a BCD-encoded value from the reader at the current position.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/reader.h>

dmi_reader_get_bcd(reader, type, pvalue)
----

== Description

The `dmi_reader_get_bcd` macro reads `sizeof(type)` bytes from the reader,
decodes the value from BCD encoding using `dmi_decode_bcd`, and stores the
result in the variable pointed to by `pvalue`. The reader cursor is advanced.

=== Parameters

`reader`::
Reader to read from.

`type`::
Wire-format type to read (e.g., `dmi_byte_t`).

`pvalue`::
Pointer to the variable that receives the decoded value.

== Return value

Returns `true` on success, `false` if there are not enough bytes remaining.

== See also

`dmi_reader_get_bcd_at`(3),
`dmi_reader_get`(3),
`dmi_reader_get_bytes`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
