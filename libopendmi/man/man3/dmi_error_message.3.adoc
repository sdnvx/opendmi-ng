= dmi_error_message(3)
The OpenDMI contributors
OpenDMI 0.4.0

:doctype: manpage
:release-version: 0.4.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_error_message - get the human-readable message for an error reason code.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/error.h>

const char *dmi_error_message(dmi_error_code_t reason);
----

== Description

The `dmi_error_message` function returns a human-readable string describing
the error identified by `reason`. The returned pointer refers to a static
string; the caller must not free or modify it.

If `reason` is out of range or has no associated message, the function
returns `"Unknown error"`.

=== Parameters

`reason`::
Error reason code.

== Return value

Returns a pointer to a static null-terminated string. Never returns `nullptr`.

== Examples

[source, c]
```
dmi_error_t *error = dmi_error_get_last(context);
if (error != nullptr) {
    fprintf(stderr, "error: %s\n", dmi_error_message(error->reason));
    dmi_error_clear(context);
}
```

== See also

`dmi_error_raise`(3),
`dmi_error_get_first`(3),
`dmi_error_get_last`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
