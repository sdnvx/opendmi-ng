= dmi_stream_decode_str_at(3)
The OpenDMI contributors
OpenDMI 0.5.0

:doctype: manpage
:release-version: 0.5.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_stream_decode_str_at - read and decode an SMBIOS string reference from the stream at a given offset.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/stream.h>

dmi_stream_decode_str_at(stream, offset, pvalue)
----

== Description

The `dmi_stream_decode_str_at` macro reads a `dmi_string_t` index at `offset`
from the beginning of the entity body and resolves it to a string pointer via
`dmi_entity_string`. The stream cursor is not modified.

=== Parameters

`stream`::
Stream to read from.

`offset`::
Byte offset from the beginning of the entity body.

`pvalue`::
Pointer to a variable that receives the resolved string.

== Return value

Returns `true` on success, `false` if the requested range exceeds the stream
bounds.

== See also

`dmi_stream_decode_str`(3),
`dmi_stream_decode_at`(3),
`dmi_entity_string`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
