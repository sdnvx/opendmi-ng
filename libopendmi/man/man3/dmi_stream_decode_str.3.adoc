= dmi_stream_decode_str(3)
The OpenDMI contributors
OpenDMI 0.4.1

:doctype: manpage
:release-version: 0.4.1
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_stream_decode_str - read and decode an SMBIOS string reference from the stream at the current position.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/stream.h>

dmi_stream_decode_str(stream, pvalue)
----

== Description

The `dmi_stream_decode_str` macro reads a `dmi_string_t` index from the stream
and resolves it to a string pointer via `dmi_entity_string`. The stream cursor
is advanced.

=== Parameters

`stream`::
Stream to read from.

`pvalue`::
Pointer to a variable that receives the resolved string.

== Return value

Returns `true` on success, `false` if there are not enough bytes remaining.

== See also

`dmi_stream_decode_str_at`(3),
`dmi_stream_decode`(3),
`dmi_entity_string`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
