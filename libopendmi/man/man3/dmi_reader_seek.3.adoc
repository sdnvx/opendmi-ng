= dmi_reader_seek(3)
The OpenDMI contributors
OpenDMI 0.5.0

:doctype: manpage
:release-version: 0.5.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_reader_seek - seek to a byte position within a reader.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/reader.h>

bool dmi_reader_seek(dmi_reader_t *reader, size_t position);
----

== Description

The `dmi_reader_seek` function repositions the reader cursor to `position`
bytes from the beginning of the range.

The end of the range is accepted, since it is where the cursor stands once
everything has been read, and a range may be read from there with nothing
left in it.

=== Parameters

`reader`::
Reader to reposition.

`position`::
Byte offset from the beginning of the range.

== Return value

`true` on success, `false` if `position` is past the end of the range.

== See also

`dmi_reader_tell`(3),
`dmi_reader_reset`(3),
`dmi_reader_mark`(3),
`dmi_reader_rewind`(3),
`dmi_reader_skip`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
