= dmi_stream_read_data(3)
The OpenDMI contributors
OpenDMI 0.4.1

:doctype: manpage
:release-version: 0.4.1
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_stream_read_data - read data from the stream at the current position.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/stream.h>

bool dmi_stream_read_data(dmi_stream_t *stream, void *ptr, size_t length);
----

== Description

The `dmi_stream_read_data` function copies `length` bytes from the stream into
the buffer pointed to by `ptr` and advances the stream cursor. No byte-order
conversion is performed.

=== Parameters

`stream`::
Stream to read from.

`ptr`::
Buffer that receives the data; must be at least `length` bytes.

`length`::
Number of bytes to read.

== Return value

Returns `true` on success, `false` if there are fewer than `length` bytes
remaining in the stream.

== See also

`dmi_stream_read`(3),
`dmi_stream_read_data_at`(3),
`dmi_stream_decode`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
