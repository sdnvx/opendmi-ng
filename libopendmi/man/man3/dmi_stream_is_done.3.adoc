= dmi_stream_is_done(3)
The OpenDMI contributors
OpenDMI 0.5.0

:doctype: manpage
:release-version: 0.5.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_stream_is_done - check whether all data in the stream has been consumed.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/stream.h>

bool dmi_stream_is_done(const dmi_stream_t *stream);
----

== Description

The `dmi_stream_is_done` function checks whether the stream cursor has reached
the end of the entity body, meaning there are no remaining bytes to read.

=== Parameters

`stream`::
Stream to check.

== Return value

Returns `true` if there are no remaining bytes to read, `false` otherwise.

== See also

`dmi_stream_initialize`(3),
`dmi_stream_seek`(3),
`dmi_stream_reset`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
