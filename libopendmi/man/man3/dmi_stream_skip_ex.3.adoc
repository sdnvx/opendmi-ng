= dmi_stream_skip_ex(3)
The OpenDMI contributors
OpenDMI 0.5.0

:doctype: manpage
:release-version: 0.5.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_stream_skip_ex - advance the stream cursor from a mark.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/stream.h>

bool dmi_stream_skip_ex(
        dmi_stream_t      *stream,
        dmi_stream_mark_t  from,
        size_t             length);
----

== Description

The `dmi_stream_skip_ex` function places the stream cursor `length` bytes past
the position of `from`, a mark taken earlier with `dmi_stream_mark`(3).

Records of a declared length are stepped over by that length rather than by the
number of bytes read from them, which is what this expresses: the beginning of
the record is marked, the fields the record is known to hold are read, and the
cursor is then placed at the next record. A record longer than the fields the
decoder knows is handled without arithmetic, and adding a field to the decoder
does not change how the next record is found.

Counting from the current position rather than from a mark is
`dmi_stream_skip`(3), and coming back to a mark without advancing is
`dmi_stream_rewind`(3).

=== Parameters

`stream`::
Stream to advance.

`from`::
Mark to count `length` from, taken from the same stream with
`dmi_stream_mark`(3).

`length`::
Number of bytes to advance by.

== Return value

Returns `true` on success, `false` if `stream` is `nullptr`, if the resulting
position is beyond the end of the entity body, or if `from` was taken from a
stream reading another entity.

== Examples

[source, c]
----
// Descriptors are two bytes long, and longer ones are allowed for future
// extensions, so the next one is found by the declared length
for (size_t i = 0; i < descriptor_count; i++) {
    dmi_stream_mark_t start = dmi_stream_mark(stream);

    bool status =
        dmi_stream_has(stream, descriptor_length) and
        dmi_stream_decode(stream, dmi_byte_t, &descriptor->type) and
        dmi_stream_decode(stream, dmi_byte_t, &descriptor->data_format) and
        dmi_stream_skip_ex(stream, start, descriptor_length);

    if (not status)
        return dmi_entity_incomplete(entity);
}
----

== See also

`dmi_stream_mark`(3),
`dmi_stream_rewind`(3),
`dmi_stream_seek`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
