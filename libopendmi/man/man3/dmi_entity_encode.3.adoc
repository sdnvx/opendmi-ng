= dmi_entity_encode(3)
The OpenDMI contributors
OpenDMI 0.5.0

:doctype: manpage
:release-version: 0.5.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_entity_encode - encode the SMBIOS structure an encoder has been initialized with.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/entity.h>

bool dmi_entity_encode(dmi_encoder_t *encoder);
----

== Description

The `dmi_entity_encode` function writes a structure back into the bytes it
is made of, undoing what `dmi_entity_decode`(3) has done.

Structures whose specification describes their layout are encoded by its
fields, see `dmi_fields_encode`(3), and the rest by the encoding handler of
the specification. Structures which carry no data the model holds, such as
the end-of-table one, and the ones of the types no specification describes,
are kept as they are in the preserve mode: their bytes are all the model the
structure has.

The encoder is set up with `dmi_encoder_initialize`(3), which writes the
header, and the formatted area is completed with `dmi_encoder_finish`(3).

=== Parameters

`encoder`::
Encoder of the structure, see `dmi_encoder_initialize`(3).

== Return value

`true` if the structure has been encoded, `false` otherwise.

== Errors

`DMI_ERROR_NULL_ARGUMENT`::
Encoder is `nullptr`.

`DMI_ERROR_INVALID_STATE`::
Structure cannot be encoded: its type has no specification and the mode is
the canonical one, or its specification decodes it by a handler with no
encoding one.

`DMI_ERROR_OUT_OF_MEMORY`::
Buffers of the encoder cannot grow.

== See also

`dmi_entity_decode`(3),
`dmi_fields_encode`(3),
`dmi_encoder_initialize`(3),
`dmi_encoder_finish`(3),
`dmi_save`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
