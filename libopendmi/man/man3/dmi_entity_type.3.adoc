= dmi_entity_type(3)
The OpenDMI contributors
OpenDMI 0.5.0

:doctype: manpage
:release-version: 0.5.0
:manmanual: OpenDMI Manual
:mansource: OpenDMI {release-version}

== Name

dmi_entity_type - get entity SMBIOS type.

== Library

OpenDMI C library (`libopendmi`, `-lopendmi`)

== Synopsis

[source, c]
----
#include <opendmi/entity.h>

dmi_type_t dmi_entity_type(const dmi_entity_t *entity);
----

== Description

The `dmi_entity_type` function returns the SMBIOS structure type of the given
entity. SMBIOS types 0 through 127 are defined by the SMBIOS specification;
types 128 through 255 are available for OEM- and system-specific information.

=== Parameters

`entity`::
Entity descriptor.

== Return value

Returns the SMBIOS type of the entity. If `entity` is `nullptr`, returns
`DMI_TYPE_INVALID`.

== See also

`dmi_entity_handle`(3),
`dmi_entity_name`(3),
`dmi_entity_data`(3),
`dmi_entity_info`(3)

== Copying

Copyright (c) 2025-2026, The OpenDMI contributors. Free use of this software
is granted under the terms of the BSD 3-Clause License.
